# Installiere optionale Dependencies (inkl. nativer Binaries)
# WICHTIG: optional=false zwingt npm, optionale Dependencies zu installieren
# Dies ist notwendig für lightningcss native Binaries in Docker
optional=false

# Stelle sicher, dass native Module für die richtige Plattform installiert werden
engine-strict=false
