# Installiere optionale Dependencies (inkl. nativer Binaries)
optional=true

# Stelle sicher, dass alle Plattform-spezifischen Pakete installiert werden
# Dies ist wichtig für lightningcss und andere native Module
include=optional
